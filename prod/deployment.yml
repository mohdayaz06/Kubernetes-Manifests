apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudkart-app
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudkart-app
  template:
    metadata:
      labels:
        app: cloudkart-app
    spec:
      containers:
      - name: cloudkart-app
        image: mohdayazz/cloudkart:latest
        ports:
        - containerPort: 3000
